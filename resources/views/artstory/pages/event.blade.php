<x-artstory.layout :site-settings="$siteSettings" :title="$event->name" active="events">
    <main>
        <section class="relative isolate min-h-[460px] overflow-hidden bg-[#111318] text-white md:min-h-[560px]">@if ($event->backgroundImageUrl())<img src="{{ $event->backgroundImageUrl() }}" alt="" class="absolute inset-0 -z-10 h-full w-full object-cover opacity-55">@endif<div class="absolute inset-0 -z-10 bg-gradient-to-r from-black/80 via-black/45 to-black/10"></div><div class="mx-auto flex min-h-[460px] max-w-7xl items-end px-6 py-16 md:min-h-[560px] md:py-24"><div class="max-w-4xl"><p class="text-xs font-black uppercase tracking-[0.28em] text-[#d4ca83]">{{ $event->content_type === 'pr' ? 'PR / News' : 'ART Story Event' }} @if($event->start_date) <span class="text-white/50">/</span> {{ $event->start_date->format('d M Y') }}@endif</p><h1 class="mt-5 font-display text-5xl font-semibold leading-[1.05] md:text-7xl">{{ $event->name }}</h1>@if($event->excerpt)<p class="mt-6 max-w-2xl text-lg leading-8 text-white/80">{{ $event->excerpt }}</p>@endif</div></div></section>
        <section class="mx-auto grid max-w-7xl grid-cols-1 gap-12 px-6 py-16 md:py-24 lg:grid-cols-[1fr_300px]"><article class="prose max-w-none prose-headings:font-display prose-headings:font-semibold prose-p:text-gray-700 prose-a:text-[#77773e]">{!! $event->details !!}</article><aside class="h-fit border-t-4 border-[#88884d] bg-[#f5f4f1] p-7"><p class="text-xs font-black uppercase tracking-[0.2em] text-[#88884d]">At a glance</p>@if($event->start_date)<div class="mt-6 border-b border-gray-300 pb-5"><p class="text-xs font-bold uppercase tracking-[0.15em] text-gray-500">Date</p><p class="mt-2 text-sm text-gray-800">{{ $event->start_date->format('d M Y') }} @if($event->end_date)<br>to {{ $event->end_date->format('d M Y') }}@endif</p></div>@endif @if($event->location)<div class="border-b border-gray-300 py-5"><p class="text-xs font-bold uppercase tracking-[0.15em] text-gray-500">Location</p><p class="mt-2 text-sm text-gray-800">{{ $event->location }}</p></div>@endif @if($event->external_url)<a href="{{ $event->external_url }}" class="mt-6 inline-flex items-center text-xs font-black uppercase tracking-[0.14em] text-black underline underline-offset-8">More information <span class="ml-2 text-base">→</span></a>@endif</aside></section>
        @if ($event->galleryImageUrls())<section class="bg-[#f5f4f1] py-16 md:py-24"><div class="mx-auto max-w-7xl px-6"><div class="border-b border-gray-300 pb-7"><p class="text-xs font-black uppercase tracking-[0.24em] text-[#88884d]">Visual journal</p><h2 class="mt-3 font-display text-4xl font-semibold text-black md:text-5xl">Inside the story</h2></div><div class="mt-10 columns-1 gap-6 md:columns-2 lg:columns-3">@foreach($event->galleryImageUrls() as $image)<img src="{{ $image }}" alt="{{ $event->name }}" class="mb-6 w-full break-inside-avoid bg-white object-cover">@endforeach</div></div></section>@endif
        <section class="border-t border-gray-200 bg-white py-12"><div class="mx-auto max-w-7xl px-6"><a href="{{ route('events.index') }}" class="text-xs font-black uppercase tracking-[0.15em] text-black underline underline-offset-8">← Back to Events & PR</a></div></section>
    </main>
</x-artstory.layout>
