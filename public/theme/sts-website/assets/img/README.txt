Drop media here.

sts-at-a-glance.mp4   hero film (takes over the animated fallback automatically)
hero-poster.jpg       still shown before the film loads
glance-1..4.jpg       optional stills for the animated fallback scenes

Everything here is optional. Missing files fall back to designed gradients.
